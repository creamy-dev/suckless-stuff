# Modded Suckless Utils
This is my modded suckless utils.
## Building
Install the following packages:
```bash
sudo apt install build-essential libx11-dev libxinerama-dev libxft-dev git 
```
Then:
```bash
chmod +x build.sh
./build.sh
```
